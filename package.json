{
	"name": "obsidian-save-console-log",
	"version": "0.0.0",
	"description": "Save the log from the Obsidian developer console to a file. Particularly helpful on mobile, where the developer console cannot be easily accessed.",
	"main": "src/main.ts",
	"scripts": {
		"dev": "yarn set-versions && obsidian-plugin dev src/main.ts",
		"build": "yarn set-versions && obsidian-plugin build src/main.ts",
		"set-versions": "node scripts/set-versions.mjs"
	},
	"license": "CC0-1.0",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"@typescript-eslint/eslint-plugin": "5.29.0",
		"@typescript-eslint/parser": "5.29.0",
		"builtin-modules": "3.3.0",
		"eslint": "^8.18.0",
		"obsidian": "latest",
		"obsidian-plugin-cli": "^0.8.1",
		"typescript": "4.7.4"
	}
}
